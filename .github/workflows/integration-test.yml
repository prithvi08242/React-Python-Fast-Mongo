name: integration-test

on:
  workflow_run:
    workflows:
      - github-actions-ci
    types:
      - completed

jobs:
  integration-test:
    
    runs-on: ubuntu-latest
    steps:

    - name: hello world
      run: echo "Hello, world!"


    