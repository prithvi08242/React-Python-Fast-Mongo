name: integration-test

on:
  push:
    branches:
    - feature/*

jobs:
  integration-test:
    
    runs-on: ubuntu-latest
    steps:

    - name: hello world
      run: echo "Hello, world!"
      
    
    